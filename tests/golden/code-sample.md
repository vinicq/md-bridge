The block below illustrates how a short Python snippet should be preserved when converting from PDF back to Markdown.

```python
def greet(name: str) -> str:
    return f"hello, {name}"
```

```
print(greet("world"))
```

Surrounding prose continues here after the snippet.