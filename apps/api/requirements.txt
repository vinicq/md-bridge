# Runtime dependencies for the md-bridge API.
# This file mirrors the [project.dependencies] section of pyproject.toml and
# is provided for tooling that prefers a plain requirements file.
#
# Recommended install (uses pyproject and picks up dev extras when needed):
#   python -m venv .venv
#   .venv/bin/pip install -e ".[dev]"
#
# Plain pip install:
#   pip install -r requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
# python-multipart 0.0.27+ closes GHSA-mj87-hwqh-73pj, GHSA-pp6c-gr5w-3c5g,
# GHSA-wp53-j4wj-2cfg (malicious-input resource exhaustion).
python-multipart>=0.0.27
# Capped below the next minor on purpose: 1.28 shifts text extraction enough to
# trip the 2% drift gate. See the longer note in pyproject.toml.
PyMuPDF>=1.27.2.3,<1.28
# pypdf 6.10.2+ closes 22 GHSAs across the 5.x/6.x lines.
pypdf>=6.10.2
# markdown 3.8.1+ closes GHSA-5wmx-573v-2qwq.
markdown>=3.8.1
# Must stay inside the minor of the playwright/python image in
# apps/api/Dockerfile, which supplies Chromium. See pyproject.toml.
playwright>=1.49,<1.63
# markdown-to-pdf parses YAML front matter with safe_load (#150).
PyYAML>=6.0
